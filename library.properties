#include <OttoCore.h>

int main() {
    // Initialize the robot
    OttoCore_init();

    // Make the robot move forward
    OttoCore_moveForward(10);

    // Wait for a moment
    OttoCore_delay(1000);

    // Stop the robot
    OttoCore_stop();

    return 0;
}
